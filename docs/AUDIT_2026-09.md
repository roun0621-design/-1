# 2026-09 전면 점검 (대회 종료 후)

계획·진행 상태 페이지: https://claude.ai/code/artifact/c132ab2a-46b2-4e17-b2d4-6f95b260acfb

6단계: ⓪ 안전망 → ① 인증 정리 → ② WA 규정 검증 → ③ 연동 검증(명단→결과→문서→전파) → ④ 구조 정착(server.js 분해·급조 코드·매칭 통합) → ⑤ 운영 절차 기능화(데일리/시간표 업로드) → ⑥ 사용성 일관성(업로드·버튼·알림·용어·모바일).

진행 방식: 항목마다 (1) 규정/기대 동작을 옆에 적고 (2) 격리 DB 회귀 테스트를 먼저 쓰고 (3) 실패하면 이슈로 고치고 (4) 한 항목 = 한 커밋, 전체 테스트 통과 시에만 푸시.

## Phase 0 — 안전망
- 기준선: 2026-09-17 커밋 `39093c9`, 테스트 33파일 184건 통과.
- 실데이터 픽스처: `tests/fixtures/yecheon2026/` (익명화, README 참조).
- 파이프라인 회귀: `tests/flows/01_yecheon_pipeline.test.js` — 두 대회 1·2·3단계(3일) + 시간표 + 기록표를 격리 DB에 끝까지 올려 결과를 고정.
- 운영 DB 스냅샷·S3 백업: 서버에서 수동 (`cp db/competition.db db/backup_2026yecheon_final.db`, `.env` `BACKUP_S3_BUCKET`).

## 기록 (항목별 결과는 계획 페이지에, 근거·커밋은 여기에 누적)
| 날짜 | Phase | 항목 | 결과 | 커밋 |
|---|---|---|---|---|
| 2026-09-17 | 0 | 픽스처·파이프라인 테스트 (17건) | 완료 | a3dce94 |
| 2026-09-17 | 1 | JWT→레거시 키 브리지: 관리자 JWT(쿠키/Bearer)가 ~90개 키 검사를 통과 → 브라우저가 관리자 비밀번호를 저장·전송하지 않음 (표식 `jwt-session`), 기존 저장분 자동 제거, 세션 유지(40분 주기 갱신) | 완료 | (이 커밋) |
| 2026-09-17 | 1 | `trust proxy`(기본 1홉, `TRUST_PROXY` env) + XFF 직접 파싱 제거(req.ip) | 완료 | (이 커밋) |
| 2026-09-17 | 1 | `/api/_diag/*` 진단 라우트 삭제 (jwt_secret·admin_pw 미리보기 노출) | 완료 | (이 커밋) |
| 2026-09-17 | 1 | 부팅 시 JWT 비밀번호를 legacy admin_pw 로 덮어쓰던 재동기화 제거 | 완료 | e39c949 |
| 2026-09-17 | 1 | 레거시 키 로그인: IP+심판명 10회 실패→10분 잠금, login_audit 기록, 키-only 경로에서 관리자 비번 거부 | 완료 | (이 커밋) |
| 2026-09-17 | 1 | 자격증명 정책: 관리자 키 8자·운영/기록위원 키 6자·흔한 값 금지(400, 예전엔 조용히 무시), JWT 비번 8자, 약한 비번 로그인 시 변경 화면 안내, DB 실제 값 기준 보안 경고 + `/api/admin/security-status` | 완료 | (이 커밋) |
| 2026-09-17 | 1 | 로그인 이력 화면(관리자 → 사용자 관리), 잠금 해제 API, 없는 계정 응답시간 평준화, 운영키 캐시 1분 주기 재로딩, JWT 세션 8시간 유휴 로그아웃 | 완료 | (이 커밋) |
| 2026-09-17 | 2 | 종합경기 점수표: 계수 17종목 공식값 일치, 1000점 기준 기록 일치, 0.01 단위 전 구간(13만 건) 부동소수 오차 없음, 세부종목 생성 순서 고정 | 검증 완료(수정 없음) | (이 커밋) |
| 2026-09-17 | 2 | 신기록 감지 — **트랙 추풍 기록이 신기록으로 감지되던 버그**(조 풍속 heat.wind 미반영) 수정, 풍속이 나중에 입력·정정되면 재판정 | 수정 | (이 커밋) |
| 2026-09-17 | 2 | 신기록 감지 — 기준 기록이 없으면 종목 1위가 전부 신기록 대기로 올라가던 동작 제거(화면 배지와 일치시킴) | 수정 | (이 커밋) |
| 2026-09-17 | 2 | 신기록 감지 — 종합경기 풍속 세부종목 평균 +2.0 초과 시 참고기록(감지 제외) 신규 구현. 동기록·DQ·종목 최고 1명 규칙 테스트 고정 | 수정 | (이 커밋) |
| 2026-09-17 | 2·4 | 필드 순위 규칙을 공용 모듈 `public/lib/ranking.js` 하나로 통합(서버·브라우저 공용). 같은 계산이 7곳(record.js 2·dashboard.js 3·results.js 2·record-fieldpad.js·fieldCardImport.js)에 복사돼 있었음 | 수정 | (이 커밋) |
| 2026-09-17 | 2 | **높이 카운트백 오류**(TR 26.8): "마지막으로 넘은 높이까지의 실패 수"가 아니라 경기 전체 실패 수로 가리고 있었음 → 넘은 높이가 같고 그 높이 실패 수도 같은 두 선수의 순위가 규정과 달라질 수 있었음 | 수정 | (이 커밋) |
| 2026-09-17 | 2 | **3회 연속 실패**(TR 26.2): 한 높이에서 3X 일 때만 탈락 처리 → 높이에 걸친 연속 실패(X·패스·패스·XX)를 탈락으로 인식 못 해 입력 칸이 계속 열렸음 | 수정 | (이 커밋) |
| 2026-09-17 | 2 | **상위 8명**(TR 25.6): 8위와 최고 기록만 같으면 모두 진출시켰음 → 두 번째·세 번째 기록으로 먼저 가리고, 끝까지 같을 때만 모두 진출 | 수정 | (이 커밋) |
| 2026-09-17 | 2 | 종합경기 세부종목 높이 입력 화면은 카운트백 없이 최고 높이로만 순위 표시 → 공용 모듈 적용 | 수정 | (이 커밋) |
| 2026-09-17 | 2 | 다음 라운드 시드 순서(TR 20.3.2): Q 전원을 기록만으로 정렬 → 조 1위들·조 2위들… 순으로 수정(`lib/seeding.js`). 레인 그룹(TR 20.4) 3패턴은 규정과 일치 확인 | 수정/검증 | (이 커밋) |
| 2026-09-17 | 1·3 | **쓰기 가드**: 변경 라우트 40개가 무인증이었음(진행 중 종목 기록 입력·기록 초기화·결승 생성·레인·소집·풍속·진출자·릴레이 주자·.lif/.txt 가져오기). /api 의 모든 POST/PUT/PATCH/DELETE 에 유효한 키 요구(공개 경로 5종 제외), 멀티파트는 파싱 직후 검사, 클라이언트 fetch 에 키 자동 첨부 | 수정 | (이 커밋) |
| 2026-09-17 | 6 | 기록입력: 종목 목록(왼쪽 패널) 접기/펼치기 — 입력 폭 796→1132px, 상태 기기 저장 | 완료 | 6f60a93 |
| 2026-09-17 | 2 | 공식 기록 환산(TR 19.24): 1/1000초 입력값을 그대로 기록과 비교 → 1/100초 올림(도로 1초) 후 비교. 10.213 은 10.22 와 동률 | 수정 | (이 커밋) |
| 2026-09-17 | 2 | 진출자 자동 선정(TR 20.3·21): **DQ 선수도 기록이 남아 있으면 Q 로 자동 선정**되던 문제, 마지막 자리 동기록을 말없이 앞 순서로 자르던 문제 → 상태코드 제외 + 동기록 경고(심판장 결정) | 수정 | (이 커밋) |
| 2026-09-17 | 3-④ | **문서 순위 ≠ 화면 순위**: 문서 생성기 4곳(결과지 PDF·상장용 순위·전체기록 엑셀·부별 종합기록부)이 각자 순위를 계산. 거리 종목은 최고 기록만 비교(동률 순서 임의)하고 결과지는 `rank++` 라 동률도 1·2위로 갈렸으며, 높이 카운트백은 경기 전체 실패 수 → 모두 공용 모듈로 교체 | 수정 | (이 커밋) |
| 2026-09-17 | 3-④ | **상장·기록증·문자 순위(getEventResultsForCert) 재작성**: 순위=목록 위치(idx+1)라 동기록도 1·2위로 갈림, 높이 종목은 엉뚱한 테이블을 봐서 순위·기록 없음, 필드 DQ 선수가 순위에 포함, 종합경기 미지원, 기록 없는 선수에게도 순위 → 공용 규칙 + 조 내 순위(heat_rank) | 수정 | (이 커밋) |
| 2026-09-17 | 3-① | **소집 출석 오인식**: 남·여 같은 배번일 때 첫 선수를 써서 여자 100m 소집에서 "25" → 남자 25번이 다른 종목에 출석 처리. 다른 대회 선수로 새는 전역 검색도 제거 → 소집 중인 종목 등록자 우선·성별 일치 | 수정 | (이 커밋) |
| 2026-09-17 | 3-⑥ | 시간표 스마트 머지: 지난 일차만 든 파일은 0건 반영인데 성공처럼 보였음(이슈 #2) → `needs_force` 로 알리고 관리자 화면에서 강제 덮어쓰기 재업로드 제안 | 수정 | (이 커밋) |
| 2026-09-17 | 3-② | **트랙 기록 중복 행**: `UNIQUE(heat_id,event_entry_id,attempt_number)` 가 NULL(트랙)에는 효력이 없어 동시 요청이 같은 선수 행을 2개 만들 수 있었음(화면은 옛 행 표시·수정은 새 행). 부분 유니크 인덱스 + 기존 중복 정리(최신 1행 유지) + 경합 시 갱신으로 전환 | 수정 | (이 커밋) |
| 2026-09-17 | 요청 | **상장 워드(.docx) 출력**: 경기 완료된 결승 종목에 '상장 출력' 버튼(기록입력) → 1~N위(동순위 포함) 상장을 한 사람당 한 쪽 .docx 로. 문구는 상장관리 양식(제목·본문 변수·수여자) 사용, 관리자 일괄 발급에도 '워드' 형식 추가. 표준 상장 양식(제 호/상 장/종목·순위·기록·소속·성명/본문/날짜/수여자·직인) | 완료 | (이 커밋) |
| 2026-09-17 | 3-⑤ | **ID카드(AD카드) 바코드가 스캔되지 않음**: 카드의 바코드는 `|||||` 글자로 그린 모양뿐 → Code 128 로 실제 값을 인쇄(`lib/code128.js`, 의존성 없음). 값 = 등록 바코드, 없으면 배번(여자는 W 접두 — 소집실 규칙과 동일). ZXing 으로 렌더링된 PDF 에서 4장 모두 판독 확인. 계주 팀(가상 선수)이 카드로 나오던 것 제외, 예선·결승 종목 중복 표기 제거, 선수별 DB 조회(N+1) 제거, 긴 이름 자동 축소, 필터(`?team=`·`?gender=`·`?event_id=`·`?athlete_ids=`) 추가. `/api/barcode/:code` 는 `?competition_id=` 범위 지원 | 수정 | (이 커밋) |
| 2026-09-17 | 3-⑥ | **종료 잠금 누락**: 전역 잠금이 본문의 id 만 보고 대회를 찾아(라우트 매칭 전이라 `req.params` 는 비어 있음) URL 에만 id 가 있는 요청 — 풍속·경기 완료·소집 메모 등 — 은 종료된 대회에서도 운영키로 수정됨(테스트로 재현). 경로에서 id 해석 + `event_ids`/`heat_entry_id` + 멀티파트(업로드) 검사 추가. 반대로 헤더로만 키를 보내는 관리자는 막히던 것 수정. **상장·기록증 발급은 잠금 면제**(종료 뒤 시상) | 수정 | (이 커밋) |
| 2026-09-17 | 3-⑥ | **백업**: 모든 백업이 7일 뒤 삭제 → 대회 종료 시 영구 스냅샷(`backup_final<대회id>_*.db`) 생성·보존. 수동 백업 API 가 `await` 누락으로 **항상 500** 이던 것, 통복원의 사전 백업이 완료 전에 복원을 시작할 수 있던 것 수정. 테스트 실행이 `backups/` 에 임시 DB 백업을 쌓던 것 차단. 상태 API 에 S3 설정 여부·종료 스냅샷 표시 | 수정 | (이 커밋) |
| 2026-09-17 | 3-⑦ | **오프라인 큐가 모든 쓰기를 '성공'으로 위장**: 실패한 `/api` 쓰기를 전부 큐에 넣고 200 을 돌려줌 → 오프라인 로그인(비밀번호가 IndexedDB 에 저장), 기록증 문자, 결승 생성, 기록 초기화까지 나중에 혼자 실행. → 심판 입력·소집 출석 6종만 큐(허용 목록), 나머지는 503 으로 실패를 알림 | 수정 | (이 커밋) |
| 2026-09-17 | 3-⑦ | **오프라인 재전송 결함**: ① 같은 칸을 고쳐 쓰면(X→O) 첫 전송 뒤 둘째가 '서버가 더 최신'으로 거부돼 **고친 값이 버려짐** → 칸별 마지막 값만 전송 ② 재전송에 키가 없어 PATCH(소집 출석)가 403 으로 영구 실패 → 키 보관·재전송 ③ `online` 이벤트에서만 재전송(경기장 Wi-Fi 는 AP 에 붙은 채 끊겨 이벤트가 안 옴) → 페이지 로드·15초 주기·저장 성공 시 ④ 동시 실행으로 2~3중 전송 → 단일 실행 ⑤ 서버가 거부한 항목이 영원히 큐에 남고 내용도 안 보임 → 큐에서 빼고 '반영되지 않은 입력' 목록 표시 ⑥ 응답 없는 요청이 입력칸을 묶음 → 12초 제한 ⑦ 대기 건수를 2초 뒤 무조건 0 으로 표시 | 수정 | (이 커밋) |
| 2026-09-17 | 3-⑦ | **저장 실패가 화면에 안 보임**: `showBanner('저장 실패','error')` 는 인자 순서가 틀려 catch 안에서 다시 예외 → 알림 없음 + 화면엔 값이 남음. 상태코드·풍속·비고·파울 저장은 `console.error` 만. '전체 저장 완료'는 실패가 있어도 표시 → 실패 알림 + 서버 값으로 되돌림. 상수가 v131 에 멈춘 캐시 정리 블록이 **페이지를 열 때마다 현재 앱 캐시를 삭제**(오프라인 새로고침 불가) → 제거. SSE 재연결 시 기록 화면 새로고침 추가 | 수정 | (이 커밋) |
| 2026-09-17 | 3-⑧ | **문자**: 시뮬레이션 기록을 '보냄'으로 쳐서 리허설 뒤 실제 일괄 발송이 0건이 되던 것, 일괄 발송 중 다시 누르면 두 루프가 각각 보내 **유료 문자 중복**(진행 중 409 + 발송 직전 재확인), 기록증 단건 발송이 다른 대회 참가자에게도 가능·연타 중복(대회 범위 + 2분 재발송 제한) | 수정 | (이 커밋) |
| 2026-09-17 | 3-⑧ | **푸시**: 소집 완료가 조마다 같은 알림(8개 조 = 8번)·완료→되돌리기→완료 시 '결과 발표' 재발송 → 종목·종류별 6시간 중복 방지, 제목에 부(division) 표기. 관람객이 대시보드를 열 때마다 FCM 토픽 구독 3회(발송에 쓰이지 않음)·관심 종목 전체 재기록 → 제거·변경 시에만. 관리자 화면을 열면 그 기기의 관심 종목이 지워지던 것, `invalid-argument` 로 전체 토큰이 비활성화될 수 있던 것 수정 | 수정 | (이 커밋) |
| 2026-09-17 | 요청 | **워드 상장 양식 분리**: 워드 상장이 PDF 양식(문자 발송용: 배경·로고·직인 이미지)을 빌려 쓰던 것 → 현장 인쇄용 전용 양식(`award_docx_template`, `lib/awardDocxTemplate.js`). 제목·본문·계주(단체) 본문·발급번호 형식/시작·시상일·수여자·직인 자리·글꼴·글자 크기·용지·테두리·표시 항목. 대회 전용 → 전체 기본 → 내장 기본 순, 저장 없이도 바로 출력. 계주는 선수 명단 표기. 관리자 → 상장관리에 편집 카드 + 저장 전 미리보기(.docx) | 완료 | (이 커밋) |
| 2026-09-17 | 6-① | **사용성**: 업로드 칸 31곳 중 3곳만 끌어다 놓기 가능 → 공통 처리(common.js)로 전부. `alert()` 84곳 → 같은 모양의 안내창(uiAlert). 주 버튼이 취소 앞에 있던 4곳 통일(취소 · 주 버튼, 오른쪽 정렬) | 수정 | `e521a2d` |
| 2026-09-17 | 6-② | **ID카드 발급 버튼이 화면 어디에도 없었음**(서버 기능만 존재, `openADCards` 호출부 0) → 문서·다운로드 탭에 발급 카드: 소속·성별·배번 지정(`?bibs=12,W31`)·쪽당 장수·바코드. `confirm`/`prompt` 116곳 → `uiConfirm`/`uiPrompt`(위험 작업은 빨강 + 초점 취소, 키 입력은 가림). 영어 알림·'콜룸' 표기 정리. 규칙집 `docs/UX_RULES.md` | 수정 | (이 커밋) |
| 2026-09-17 | 6-③ | **계주 종목명 표기 통일**(이슈 #1·#10): 저장값이 `4X100mR` 과 `4×800mR`·`4×1500mR` 로 섞여 있던 것 → 모두 `4X…mR`. 생성 표 2곳 수정 + 부팅 마이그레이션(종목명·전광판 키 함께, 유니크 충돌 시 건너뜀). 입력은 x/X/× 모두 수용. 회귀 테스트: 실데이터 파이프라인에서 × 저장 0건 | 수정 | (이 커밋) |

## 발견한 이슈 (Phase 0 진행 중 — 해당 Phase에서 처리)
| # | Phase | 내용 | 근거 |
|---|---|---|---|
| 1 | 6 (표기 통일) | 연맹 명단 가져오기가 릴레이 종목명을 `4X100mR`·`4X400mR(Mixed)`(대문자 X)와 `4×1500mR`·`4×800mR`(곱셈 기호)로 섞어 생성 → 시간표·.lif·문서에서 이름 정규화에 의존 | 파이프라인 테스트 `norm()` 우회 |
| 2 | 3-⑥ | 시간표 스마트 머지가 "오늘" 기준으로 지난 일차를 보존해, 대회 종료 후엔 어떤 시간표도 올라가지 않음(응답은 200 "보존") — 대회 날짜가 아니라 실제 날짜에 묶여 있고, 사용자에게 "0건 반영"이 성공처럼 보임 | 테스트에서 `overwrite_mode=force` 필요 |
| 3 | 4 | 테스트 파일 전체가 임시 DB 하나를 공유하고 실행 순서는 vitest 가 소요시간으로 정함 → 전역 데이터(NR/DR 기록 등)를 넣는 테스트가 서로를 깨뜨릴 수 있음. 파일별 DB 분리 또는 공통 정리 규칙 필요 | 24_records_bulk 가 파이프라인 테스트 뒤에 돌면 '신규'→'update' 로 실패했음 |
| 4 | 1 | (남은 일) URL 의 `?key=` 는 이제 비밀이 아닌 표식이지만, 레거시 운영키로 로그인한 심판 세션은 여전히 실제 키를 쿼리로 보냄 → 운영키도 세션/헤더로 전환 필요. legacy 관리자 비밀번호는 x-admin-key 로 여전히 유효(키-only 로그인 경로 포함) → 폐기 일정 필요 | 브리지 도입 후 잔여 |

## 운영 메모 (배포 시)
- `.env` 에 `TRUST_PROXY` 확인: nginx 등 프록시 1대 뒤면 기본값(1) 그대로, 프록시가 없으면 `TRUST_PROXY=false`.
- 배포 후 관리자들은 한 번 로그아웃→로그인 권장(자동 교체되지만, 쿠키가 만료된 브라우저는 재로그인 필요).
| 6 | 1 (결정 필요) | 운영키 평문 저장·목록 표시: 해시로 바꾸면 관리자가 "심판에게 키 다시 알려주기"를 못 하게 됨(발급 시 1회만 표시). 운영 방식 결정 후 진행 | 보류 |
| 7 | 1 (남음) | CSP(nonce) 도입·토큰 localStorage 제거·`/api/registered-judges` 무인증 노출 — 인라인 스크립트가 많은 현 구조에선 Phase 4(구조)·6(공통 셸)과 함께 진행 | 대기 |
| 8 | 2 | 종합경기 점수식이 세부종목 '이름'이 아니라 '순서'로 선택됨 → 순서가 어긋나면 조용히 틀린 점수(멀리뛰기에 포환 식). 현재는 생성 순서가 규정과 일치함을 테스트로 고정. 이름 기반 선택으로 바꾸는 건 Phase 4 | 관찰 |
| 9 | 2 | 수동 계측(핸드 타이밍) 보정(+0.24/+0.14)과 기록 공인 조건(계측 방식·고도)은 시스템에 개념이 없음 — 전자계측 전제. 필요 시 결정 | 결정 필요 |
| 10 | 6 | 이슈 #1 보충: 릴레이 표기 혼재는 매핑 표 2곳(server.js 2043·6086)에 의도적으로 박혀 있고 전광판 키·기록표 매칭이 이 이름에 묶여 있음 → 표기 통일은 DB 마이그레이션과 함께 Phase 6 용어 정리에서 | 관찰 |
| 11 | 4 | `tests/api/32_field_card_transcribe` 가 전체 실행에서 간헐 실패(단독 실행은 통과) — 픽스처 파일/타이밍 경합 추정. 테스트 DB 공유(#3)와 함께 정리 | 관찰 |
| 12 | 3-⑦ (남음) | 오프라인 재전송의 풍속·소집 상태는 서버 쪽 최신 검사(offline_input_at)가 없어 다른 기기의 이후 수정값을 덮을 수 있음. 충돌 판정이 기기 시계에 의존(시계가 틀린 태블릿). → 서버 `updated_at` 기반 버전 검사로 전환 필요 | 관찰 |
| 13 | 3-⑧ (남음) | 문자 월 한도(`monthly_quota`)는 저장만 되고 어디서도 검사하지 않음. 전화번호 형식 검증 없음(`+8210…` 은 `010…` 과 다른 사람으로 저장). 푸시 관심 종목 키가 `성별|종목명` 이라 부(division)를 구분하지 않음(화면 즐겨찾기와 같은 단위) | 관찰 |
| 14 | 6 (일부 해결) | ID카드 발급 화면 추가됨(6-②). 남은 것: 글자 크기·띠 색·표시 항목 설정 화면(관리자가 예전에 의도적으로 삭제) —(장수/쪽, 글자 크기, 띠 색, 표시 항목은 DB 기본값뿐) · 팀/선수 선택 발급 UI 없음(API 필터만 추가됨) | 대기 |

