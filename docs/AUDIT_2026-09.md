# 2026-09 전면 점검 (대회 종료 후)

계획·진행 상태 페이지: https://claude.ai/code/artifact/c132ab2a-46b2-4e17-b2d4-6f95b260acfb

6단계: ⓪ 안전망 → ① 인증 정리 → ② WA 규정 검증 → ③ 연동 검증(명단→결과→문서→전파) → ④ 구조 정착(server.js 분해·급조 코드·매칭 통합) → ⑤ 운영 절차 기능화(데일리/시간표 업로드) → ⑥ 사용성 일관성(업로드·버튼·알림·용어·모바일).

진행 방식: 항목마다 (1) 규정/기대 동작을 옆에 적고 (2) 격리 DB 회귀 테스트를 먼저 쓰고 (3) 실패하면 이슈로 고치고 (4) 한 항목 = 한 커밋, 전체 테스트 통과 시에만 푸시.

## Phase 0 — 안전망
- 기준선: 2026-09-17 커밋 `39093c9`, 테스트 33파일 184건 통과.
- 실데이터 픽스처: `tests/fixtures/yecheon2026/` (익명화, README 참조).
- 파이프라인 회귀: `tests/flows/01_yecheon_pipeline.test.js` — 두 대회 1·2·3단계(3일) + 시간표 + 기록표를 격리 DB에 끝까지 올려 결과를 고정.
- 운영 DB 스냅샷·S3 백업: 서버에서 수동 (`cp db/competition.db db/backup_2026yecheon_final.db`, `.env` `BACKUP_S3_BUCKET`).

## 기록 (항목별 결과는 계획 페이지에, 근거·커밋은 여기에 누적)
| 날짜 | Phase | 항목 | 결과 | 커밋 |
|---|---|---|---|---|
| 2026-09-17 | 0 | 픽스처·파이프라인 테스트 (17건) | 완료 | (이 커밋) |

## 발견한 이슈 (Phase 0 진행 중 — 해당 Phase에서 처리)
| # | Phase | 내용 | 근거 |
|---|---|---|---|
| 1 | 3-① / 4 | 연맹 명단 가져오기가 릴레이 종목명을 `4X100mR`·`4X400mR(Mixed)`(대문자 X)와 `4×1500mR`·`4×800mR`(곱셈 기호)로 섞어 생성 → 시간표·.lif·문서에서 이름 정규화에 의존 | 파이프라인 테스트 `norm()` 우회 |
| 2 | 3-⑥ | 시간표 스마트 머지가 "오늘" 기준으로 지난 일차를 보존해, 대회 종료 후엔 어떤 시간표도 올라가지 않음(응답은 200 "보존") — 대회 날짜가 아니라 실제 날짜에 묶여 있고, 사용자에게 "0건 반영"이 성공처럼 보임 | 테스트에서 `overwrite_mode=force` 필요 |
| 3 | 4 | 테스트 파일 전체가 임시 DB 하나를 공유하고 실행 순서는 vitest 가 소요시간으로 정함 → 전역 데이터(NR/DR 기록 등)를 넣는 테스트가 서로를 깨뜨릴 수 있음. 파일별 DB 분리 또는 공통 정리 규칙 필요 | 24_records_bulk 가 파이프라인 테스트 뒤에 돌면 '신규'→'update' 로 실패했음 |
